<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.thais.pilojinha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.CadastroUsuario" />
        <activity android:name=".View.MenuLoja" />
        <activity android:name=".View.Carrinho" />
        <activity android:name=".View.CategoriaFeminino" />
        <activity android:name=".View.CategoriaInfantilMasc" />
        <activity android:name=".View.CategoriaMasculino" />
        <activity android:name=".View.PerfilUsuario" />
        <activity android:name=".View.CategoriaInfantilFeme"></activity>
    </application>

</manifest>