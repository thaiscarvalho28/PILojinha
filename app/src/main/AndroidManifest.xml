<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.thais.pilojinha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.ActivLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.ActivCadastroUsuario" />
        <activity android:name=".View.ActivMenuLoja" />
        <activity android:name=".View.ActivCarrinho" />
        <activity android:name=".View.ActivLoja" />
        <activity android:name=".View.ActivPerfilUsuario" />
        <activity android:name=".View.ActivAlertDialog" />
        <activity android:name=".View.ActivListaProdutosAdapter"></activity>
    </application>

</manifest>